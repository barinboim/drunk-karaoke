.DS_Store
.sites-runtime/
dist/media/*
!dist/media/backdrops/
dist/data/song.json
жуки*/
node_modules/
.cache/
# индекс песен: коммитим, только если в нём нет абсолютных путей (см. scripts/library.mjs)
dist/data/library.json
Alliance*/
Альянс*/
UltraStar*/



# референсы: кадры чужих караоке-дисков, публикации не подлежат
inspo/


# CMUdict: 3.6 МБ, собирается npm run cmudict
dist/data/cmudict.json
dist/data/pronunciations-*.json
.cache/ipa-dict/

# Песни живут в отдельной ветке songs и подмешиваются при сборке сайта.
# Локально этой папки как будто нет: правки и коммиты её не касаются.
dist/songs/

# список датасетов строится сам: локально сервером, на сайте сборкой
dist/corpora/index.json

# Агенты иногда скачивают сюда сторонние выгрузки: в репозиторий им нельзя
.corpus-source/
